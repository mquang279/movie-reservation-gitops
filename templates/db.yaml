{{- range $name, $db := .Values.databases }}
---
apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: {{ $name }}
  labels:
    app: {{ $name }}

spec:
  serviceName: {{ $name }}
  replicas: 1

  selector:
    matchLabels:
      app: {{ $name }}

  template:
    metadata:
      labels:
        app: {{ $name }}

    spec:
      containers:
        - name: {{ $db.image.name }}
          image: {{ $db.image.name }}:{{ $db.image.tag }}
          imagePullPolicy: {{ $db.image.pullPolicy }}

          ports:
            - containerPort: {{ $db.service.port }}

          env:
            - name: POSTGRES_DB
              value: postgres

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ $db.secret.name }}
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $db.secret.name }}
                  key: POSTGRES_PASSWORD

          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data

  volumeClaimTemplates:
    - metadata:
        name: postgres-data

      spec:
        accessModes:
          - ReadWriteOnce

        resources:
          requests:
            storage: {{ $db.resource.storage }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $db.secret.name }}
type: Opaque
stringData:
  POSTGRES_USER: {{ $db.secret.username | quote }}
  POSTGRES_PASSWORD: {{ $db.secret.password | quote }}
---
apiVersion: v1
kind: Service

metadata:
  name: {{ $name }}

spec:
  clusterIP: None

  selector:
    app: {{ $name }}

  ports:
    - port: {{ $db.service.port}}
      targetPort: {{ $db.service.targetPort}}

{{- end}}